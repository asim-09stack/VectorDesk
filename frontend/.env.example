# Base URL of the backend API.
# In development the Vite dev server proxies "/api" to the backend, so the
# default relative value works out of the box. Override for production builds.
VITE_API_URL=/api
